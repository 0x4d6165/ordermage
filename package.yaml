name: ordermage

ghc-options: -Wall

tests:
  spec:
    main: Spec.hs
    source-dirs:
      - test
    dependencies:
      - bytestring
      - hspec
      - http-client
      - http-types
      - ordermage
      - servant-client
      - warp >= 3.2.4

library:
  source-dirs:
    - src

executables:
  ordermage:
    main: exe/Main.hs
    dependencies:
      - ordermage

dependencies:
  - aeson
  - base
  - bytestring
  - email-validate >= 2
  - envy
  - opaleye >= 0.5
  - postgresql-simple
  - servant
  - servant-server
  - text
  - time
  - transformers
  - uuid >= 1.3
  - wai
  - warp

